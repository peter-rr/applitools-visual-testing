"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createUniqueFolder = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
function createUniqueFolder(basePath, name) {
    const uniquePath = path_1.default.resolve(basePath, `${name}-${Date.now()}`);
    return createUniqueFolderAtPath(uniquePath);
}
exports.createUniqueFolder = createUniqueFolder;
function createUniqueFolderAtPath(basePath) {
    if (fs_1.default.existsSync(basePath)) {
        const match = path_1.default.basename(basePath).match(/_(\d+)$/);
        const currIndex = match ? Number(match[1]) + 1 : 1;
        const newPath = currIndex > 1 ? basePath.replace(/_\d+$/, `_${currIndex}`) : `${basePath}_1`;
        return createUniqueFolderAtPath(newPath);
    }
    else {
        fs_1.default.mkdirSync(basePath, { recursive: true });
    }
    return basePath;
}
