import type { Core } from './types';
import { type Logger } from '@applitools/logger';
type Options = {
    agentId?: string;
    concurrency?: number;
    logger?: Logger;
    offlineLocationPath?: string;
};
export declare function makeCore({ agentId, concurrency, logger: defaultLogger, offlineLocationPath, }: Options): Core;
export {};
